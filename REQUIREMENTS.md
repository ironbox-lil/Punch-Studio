# 赛博打孔器：产品需求

更新日期：2026-09-20

## 1. 作品形式

输入一张摄影底图和一张色纸，用指定轮廓从底图中取出真实图像碎片，底图孔位以色纸内容回填，取出的碎片在色纸上重新排列。支持双联拼接，或把色纸扩展在照片四周。

每个孔与一块照片碎片对应，碎片保留源位置的真实像素、纹理和光影。照片打孔位置和色纸上碎片位置分别控制。不能以完整照片缩略图、随机纹理或模型生成内容替代取出的局部。

## 2. 已确认的功能范围

- 用户上传底图。
- 色纸可上传，也可通过色板或 HEX 颜色直接生成。
- 用户可以选择内置形状，也可以留空，由 DeepSeek 推荐。
- 色纸与形状的缺失分别判断，模型只补齐缺失项。
- 照片打孔布局与碎片重排布局独立设置。
- 使用预设比例或自由比例；未指定比例时自动居中裁切，用户可以自由调整选区。
- 根据单幅画面比例自动选择上下或左右拼接。
- 可选择在照片外围增加一圈纯色区域，宽度可调，碎片排列在外围。
- 照片孔位及色纸上的碎片均支持鼠标、触控拖动和键盘微调，两侧独立调整。
- LLM 使用 DeepSeek API，密钥仅用于服务端配置。
- 用户可从界面配置自己的 DeepSeek 或 OpenAI 兼容视觉模型、API 根地址和 API Key，本机持久保存。

## 3. 底图与色纸

### 3.1 底图

用户上传底图后，先处理 EXIF 方向，再计算宽高比。原文件保持不变，预览、裁切及导出使用派生画面。

第一版建议支持 JPEG、PNG、WebP。其他格式是否支持，可在实现阶段根据解码能力补充。

素材库尚未确认。若以后增加“选择素材”，选中素材应与上传图片进入同一处理流程。

### 3.2 色纸

两种来源均视为用户已经提供色纸，不应再次触发底色推荐：

1. 手动选择颜色或填写 HEX 色值，生成均匀色纸。
2. 上传色纸图像，默认保留图像纹理，并提供“仅提取底色”选项。

双联模式的色纸与裁切后的底图使用相同画面尺寸。色纸图片按覆盖画面的方式等比裁切，不拉伸。采用纹理色纸时，照片孔位的回填应使用原始色纸内容，不能包含已经放上色纸的照片碎片。外围模式只使用纯色，上传色纸时提取主色，用户可重新选色。

## 4. 内置形状

第一版至少包含以下固定选项，模型必须从中选择：

| ID        | 形状   |
| --------- | ------ |
| circle    | 圆点   |
| square    | 方块   |
| teardrop  | 水滴   |
| starburst | 星芒   |
| cross     | 十字   |
| eye       | 眼睛   |
| swallow   | 燕子   |
| puzzle    | 拼图块 |

每组作品默认采用一种形状。眼睛等具有内部镂空的形状必须保留内部空隙；只有形状实体部分参与交换。

燕子采用用户提供的侧飞剪影轮廓，制作成独立矢量蒙版；只包含燕子，不包含素材图片的白底、水印、编号或其他文字。

模型不生成新的轮廓、SVG 或自定义图形。

## 5. 比例与自动裁切

以下为根据用户指定的 16:9、3:4、1:1 补充的初始预设，可在实现中调整：

- 横向：16:9、3:2、4:3、5:4。
- 方形：1:1。
- 纵向：4:5、3:4、2:3、9:16。

默认选项为“自动”。设原图宽高比为 r，候选比例为 q，则保留面积比例为 min(r/q, q/r)。自动模式选择保留面积最大的候选，也就是裁切损失最小的比例；原图精确匹配预设时不裁切。

默认居中裁切，不变形、不拉伸。支持用户自由裁剪：

- 独立裁剪窗口，显示完整原图与选区，拖动选区移动位置，八个边角手柄调整范围。
- 自由比例允许任意矩形；选择预设比例后，调整手柄保持该比例。
- 提供缩放滑杆、当前选区像素尺寸、重置、取消和应用。选区与手柄支持方向键微调，Shift 加速。
- 鼠标与触控使用同一 Pointer Events 交互；选区始终位于原图范围内，不能翻转或出现空白边缘。
- 原图不修改。确认前仅改变裁剪草稿，取消或 Esc 不影响作品及已有推荐；更换底图重置旧选区。
- 确认后，照片预览、模型接收的图片和高清导出共享同一源选区；原图尺寸导出按选区原生像素计算。
- 自由比例的拼接方向按裁剪后实际宽高比确定。应用裁剪使旧推荐失效，但不自动调用 API。

当前不包含人脸/主体识别裁切。

用户选择具体比例后，使用该比例重新裁切，照片面板与色纸面板同步更新。比例变化不直接触发新的模型调用。

## 6. 两套独立布局

### 6.1 照片打孔布局

- 规则网格：行列数控制数量，孔位均匀排列。
- 随机分布：数量控制孔数，孔位分散在画面内。

支持调整孔的大小。自动布局不重叠、不越过边界；空间不足时缩小孔径并提示，不能无提示地少生成孔。

用户可以直接拖动任一照片孔位，或选中后使用方向键微调、Shift 加速。拖动支持鼠标与触控，不能越出照片边界；手动孔位允许重叠。拖动中按 Esc 或触控取消时恢复开始拖动前的状态。移动后，原孔位恢复原图，新孔位以色纸填充，对应碎片更新为新位置的照片内容，其目标位置和旋转不变。

提供“恢复自动孔位”。改变打孔布局、行列数、数量或孔位随机种子时恢复自动布局；更换底图清除手动孔位。裁剪、比例、拼接方式调整保留手动孔位的相对位置，必要时约束到边界内。重排色纸碎片不移动照片孔位。所有孔位编辑都不调用模型。

### 6.2 色纸上的碎片布局

- 紧凑矩阵：在色纸中央组成较小的整齐阵列，保留大面积留白。
- 自然散落：围绕画面中央随机散布，可控制聚拢程度。

外围色纸模式下，上述布局改为外围环绕排列或外围自然散落。碎片不得覆盖中央照片或越出外边界；外围空间不足时同步缩小孔与碎片，数量保持不变。

支持选择是否允许碎片旋转和重叠。重排保持碎片与源孔尺寸对应，不独立缩放碎片。

色纸上的照片碎片支持直接拖动、方向键微调、Shift 加速和 Esc/触控取消。只更新被拖动碎片的目标位置，保留其对应源孔、真实内容和旋转，其他碎片不动。拖动后原位置恢复原始色纸。此交互同时适用于上下、左右、交换顺序及外围模式；碎片始终位于色纸边界内，外围模式不得遮住中央照片。

手动碎片允许重叠，并提供“恢复自动碎片排列”。重新随机排列、改变碎片布局、散布范围、重叠设置或孔数时清空手动目标位置；仅调整源孔位置不清空。裁剪、比例和拼接变化保留相对位置并重新约束边界，更换底图重置两侧手动位置。拖动与恢复均不调用 API、不清空已有候选；PNG 导出保留修改后的目标位置。

同一形状、数量和配色可以组合出“网格打孔 + 自然散落”或“随机打孔 + 紧凑矩阵”。

提供“换一种排列”。建议分别重排照片孔位和色纸碎片，以免修改一侧时无意改变另一侧。普通参数更新应保留随机种子，避免每次预览都出现新构图。

更换颜色或形状时，尽量保留已有位置；若新形状的边界导致重叠或越界，应重新校正。

## 7. 画面拼接

自动拼接使用裁切后的单幅比例，而不是原文件比例，也不随浏览器窗口尺寸改变。

| 单幅比例      | 自动拼接方式 |
| ------------- | ------------ |
| 横向，宽 > 高 | 上下拼接     |
| 纵向，高 > 宽 | 左右拼接     |
| 方形          | 默认上下拼接 |

该规则让双联作品避免过宽或过长。建议同时提供“自动 / 上下 / 左右”选项及两幅画面的顺序切换。

默认色纸在前：上下布局时色纸在上，左右布局时色纸在左。初始不加边框或间隔，允许后续扩展。

另提供“外围色纸”：以裁剪后的照片为中心，四边扩展等宽纯色区域，颜色沿用当前底色；上传色纸时取其主色。外围宽度默认为照片短边的 28%，可在 10%–60% 间调整。照片孔位用同一纯色回填，照片碎片放在外围区域。此模式不需要交换两幅画面的顺序，单独导出的色纸 PNG 中央区域透明。

## 8. DeepSeek 推荐

### 8.1 推荐条件

“已提供色纸”包括上传色纸和手动选色；“已选择形状”包括用户明确采用的推荐形状。

推荐必须由用户点击“生成 AI 建议”或重试按钮触发。上传素材、修改/清空颜色与形状、改变比例或布局、采用候选均不得自动发起 API 请求。按钮旁显示本次建议范围和 API 费用提示；请求期间防止重复提交，失败后不自动重试。

提供颜色/色纸和形状各自的清空操作，再次点击已选色块或形状也可取消。提供“清空颜色和形状”，同时移除已上传色纸，保留底图与布局；随后由用户手动生成两项建议。未选择状态要明确显示，临时预览颜色/形状不算用户选择。无需调用 API 的内置灵感可直接使用。

下表表示用户主动生成时的补全范围：

| 已提供底图 | 已提供色纸 | 已选择形状 | 行为                 |
| ---------- | ---------- | ---------- | -------------------- |
| 是         | 是         | 是         | 直接处理，不调用模型 |
| 是         | 否         | 是         | 推荐底色，保留形状   |
| 是         | 是         | 否         | 推荐形状，保留色纸   |
| 是         | 否         | 否         | 推荐底色与形状组合   |
| 否         | 任意       | 任意       | 等待底图，不发起推荐 |

返回三组候选，每组包含缺失的配置与一句简短中文理由。只有用户采用候选后才更新对应设置。用户已经指定的配置不能被覆盖。

采用后保留同一批的三组候选，并标记当前方案。用户可反复切换来比较效果，无需再调用 API；切换只替换该批原本负责的字段，不覆盖生成前手动指定的颜色、色纸或形状。内置灵感遵循同一交互。主动重新生成时沿用该批的推荐范围。更换底图/色纸、改变裁切或手动编辑相关字段后，旧候选失效，等待用户主动生成；孔位和碎片布局变化不影响候选。

### 8.2 模型输入与输出

输入包含：

- 当前裁切后的底图预览。
- 已上传色纸的预览，或用户选定的颜色（如有）。
- 已选形状、允许选择的形状列表、哪些参数被用户指定。

输出使用结构化 JSON，底色为有效 HEX，形状为上述白名单中的 ID。服务端校验候选数量、颜色格式、形状 ID 和理由内容，并拒绝覆盖锁定参数。

模型负责图像分析、配色及形状推荐。裁切、遮罩、像素交换、布局和导出使用确定性的图像处理程序。

### 8.3 接入配置

界面入口为“LLM 配置”，允许设置服务类型、API 根地址、视觉模型名与 API Key。兼容服务需要支持 Chat Completions、图像输入及 JSON 输出。保存配置仅进行格式校验，不自动测试连接或调用模型。密钥不回显、不写浏览器存储；关闭窗口丢弃未保存草稿。

设置保存在服务端 `.local/llm.json`，优先于环境变量。留空密钥表示保留；显式清除会持久禁用模型调用，不回退旧环境密钥。更换 API 地址时需重新填写密钥。配置 API 仅接受本机连接与同源写入；修订冲突或写入失败时不覆盖现有设置。改变配置保留已完成推荐及画布，取消未完成请求。

- 服务提供方：DeepSeek。
- API 根地址：https://api.deepseek.com
- 推荐模型默认值：deepseek-flash，对应当前具备视觉能力的 DeepSeek-V4.1-Flash，可通过服务端环境变量调整。
- 旧视觉模型 deepseek-v4-flash-vision-exp 已退役；官方暂时将旧名称路由至当前 Flash。项目直接使用当前名称，不依赖旧别名。
- 使用 OpenAI 兼容的 POST /chat/completions；用户消息 content 同时包含 text 和 image_url 内容块，图片通过 Base64 data URL 传入。
- 使用 JSON 输出模式并在提示词中给出 JSON 结构；推荐任务默认关闭思考模式。不能只把图片文件名或文字描述传入来冒充图像分析。
- 通过服务端接收前端推荐请求并调用模型。
- 密钥存于本地 `.env.local` 或 `.local/llm.json`，不进入前端构建产物、需求文档或版本控制。
- `.env.example` 仅提供变量名和非敏感默认值。
- 限制重复请求；不在调整孔大小、重排或切换拼接方向时调用模型。

官方图像输入说明：https://api-docs.deepseek.com/guides/vision/

官方模型更新记录：https://api-docs.deepseek.com/updates/

2026-09-20 已完成以下验证：

1. 模型列表请求返回 HTTP 200，返回 deepseek-flash 和 deepseek-v4-pro。
2. 使用 reference 中的向日葵参考图，向 deepseek-flash 发送真实图片与文字的请求，返回 HTTP 200，响应模型名为 deepseek-flash。
3. 返回结果正确描述了向日葵、黄色花瓣、橙褐色花盘、绿色背景、深蓝色纸和水滴拼贴结构；已与参考图人工比对。
4. 返回三组配色与形状推荐，通过六位 HEX、形状白名单、数量和理由字段校验。

可复用验证脚本：scripts/check_deepseek_vision.py。该脚本会发起真实 API 请求，不属于自动化测试流程；本地图片的历史联调报告不随仓库发布。当前界面交互及缺失字段补全的自动化验收见 docs/ACCEPTANCE.md。

## 9. 导出与失败兜底

用户后续明确要求按本需求文档开发，本节一并纳入当前实现范围。

- PNG 导出完整双联或外围色纸作品。
- 分别导出打孔照片和承载碎片的色纸。
- 导出与当前预览使用相同的裁切、孔位、碎片位置、旋转和拼接参数。
- 导出尺寸明确可见，不能直接使用低分辨率预览替代高清输出。
- 分辨率设置控制照片长边，完整外围作品在照片尺寸之外加上四边扩展区域；尺寸提示与实际 PNG 一致。
- 推荐失败时，用户仍可手动选色、选形状并生成作品。
- 本地备用配色应标注为内置推荐，不应假称为 DeepSeek 的分析结果。

## 10. 当前阶段与验收要点

2026-09-20 已实现编辑界面、图像处理链路、DeepSeek 推荐 API 和 PNG 导出。具体架构与当前边界见 docs/ARCHITECTURE.md；测试与验收记录见 docs/ACCEPTANCE.md。

实现必须持续验证：

1. 四种输入组合在用户主动生成时触发正确的补全行为；未点击按钮、手动配置完整或仅编辑/清空时不调用模型。
2. 每个照片孔都对应一块从该位置提取的真实碎片；眼睛等内部镂空保留正确。
3. 自动比例选择裁切损失最小的预设，裁切结果不拉伸。
4. 横图上下拼接、竖图左右拼接、方图默认上下拼接。
5. 两侧布局可独立变化；只重排色纸碎片时，照片孔位不变。
6. 模型输出经过白名单校验，缺失字段或请求失败时保留手动制作能力。
7. 导出与预览构图一致，密钥不出现在浏览器资源或模型响应中。
8. 外围碎片始终位于纯色区域内，孔位拖动与导出坐标一致；取消拖动、触控、键盘微调和恢复自动布局行为正确。
9. LLM 配置读写不回显密钥，保存/清除不调用模型；切换服务后使用新配置且不复用旧缓存。
10. GitHub 源码不包含个人密钥、样本/参考图或生成产物；克隆后通过 `npm ci && npm run dev` 启动，缺少密钥或素材目录不妨碍手动制作。
