# 数据与密钥保护

本项目默认运行在 `127.0.0.1`，面向本机单用户使用。

- 图片在浏览器中裁剪、交换像素、排列与导出。仅主动生成建议时，压缩后的图片预览会发送给用户配置的服务。上传原文件不保存到服务端。
- API Key 由本机服务端保存到 `.local/llm.json`，以限制性权限创建并原子替换（POSIX 文件权限 `0600`）。该文件是本地明文配置，不是加密保险库；请使用操作系统账户权限保护。Windows 的实际权限由所在目录 ACL 决定。
- 配置 API 只返回服务类型、地址、模型名、是否已有密钥和修订号；密钥不回显。输入密钥只在提交时发送给同源本机服务端，关闭窗口后不保留在应用状态中，不使用 localStorage/sessionStorage。
- 更换服务地址时必须显式填写对应密钥，服务端不转发上游重定向，不回传上游错误体，不自动重试付费调用。保存配置只做格式校验，不请求模型。
- 清除密钥会持久化未配置状态，不会重新使用 `.env.local` 中旧密钥。本地配置文件损坏时启动失败，不静默切换到另一套凭证。
- 配置接口限制本机连接并校验浏览器请求来源。没有账户、多租户隔离或反向代理身份认证，不应把此工作台直接作为公共共享服务。
- `.env*`（除空值模板 `.env.example`）、`.local/`、私有样本/参考图、日常生成图片、测试报告、依赖和构建产物均不纳入 Git。`docs/images/` 仅收录选定用于公开展示的 README 案例和界面截图。开发服务器也禁止访问本地私有目录。

提交前运行 `npm run check:repo`。检查器扫描暂存区，阻止私有路径、常见凭证以及与本机密钥相同的内容进入发布源码；错误只报告文件路径，不打印命中的值。自动检查无法识别所有自定义秘密，新增配置文件时也应检查暂存区内容。

报告问题时请勿在公开 issue 中粘贴真实 API Key、配置文件、完整请求体或私人照片。可先描述复现步骤和已脱敏的错误信息。
